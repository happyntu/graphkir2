# `src/` Layout

This directory hosts the refactored implementation track.

Current policy:

* `graphkir/` and `kir/` in the repository root remain the stable baseline.
* `src/graphkir2/` is where the next-generation implementation should be built.
* New architecture work should land here first, then be benchmarked against the legacy baseline.
